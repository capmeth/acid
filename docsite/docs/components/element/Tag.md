
```html label="Structure"
<span class="element-tag {name}">
  <span class="name">
    <!-- `name` goes here -->
  </span>
  <span class="info">
    <!-- `info` goes here -->
  </span>
</span>
```

- `{name}` is the name of the tag
