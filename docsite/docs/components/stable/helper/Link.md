
```svelte mode="static" label="structure"
<a class="helper-link type-${kind}">
  if children
    <!-- render `children` -->
  else
    <span>
      <!-- display `value` -->
    </span>
  end if
</a>
```
