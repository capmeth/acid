<!--*
    Displays a pre-defined label (from configuration).

    Displays `id` as-is if not mapped in `config.labels`.

    @tags group:element

    @property { string } id
      Id of the label to display.
    @property { ...object } [vars]
      Interpolation values for the label.
-->

<span class="element-label {id}">{ t(id, vars) }</span>


<script module>
import { t } from '#frend/lib'
</script>

<script>
let { id, ...vars } = $props();
</script>
