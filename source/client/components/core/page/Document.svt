<!--*
	  Document page.

    @property { any } site
      Data from **Docsite** component.
    @property { string } uid
      Id of document asset to display.
-->

<Document 
  title={asset.title}
  section={asset.section}
  mcid={asset.mcid}
  tags={asset.tags}
  tocDepth={asset.tocDepth}
  {site}
/>


<script module>
import { context, sinfo, webpage } from '#frend/lib'

import Document from '#custom/page/Document'
</script>

<script>
let { uid, site } = $props();

let asset = sinfo.asset(uid, context.page);

context.section = asset.section;
context.asset = asset.uid; 
webpage.setTitle(asset.title);
</script>
