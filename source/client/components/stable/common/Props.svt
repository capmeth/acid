<!--*
	  Displays a list of component props.

    Nothing is rendered if `items` is nullish or empty.

    An individual prop will be skipped if it is nullish or has a truthy 
    `ignore` value.

    @tags conditional, extra-props:List, uses:main/Prop

    @property { array } items
      List of props to be rendered.
    @property { string } [type='props']
      A type for the internal **List** component.
-->

<List {items} {filter} {type} {...args}>
  {#snippet children(data)}
    <Prop {...data} />
  {/snippet}
</List>  


<script module>
import List from './List'
import Prop from '#custom/main/Prop'

let filter = data => data && !data.ignore
</script>

<script>
let { items, type = 'props', ...args } = $props();
</script>
